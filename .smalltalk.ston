SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {

      #baseline : 'FamixOpenApi',
      #directory : 'src',
      #ignoreImage : true,
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming
    }
  ],
  #testing : {
    #coverage : {
      #packages : [ 'FamixOpenApiGenerator.*' 'Famix-OpenApi.*' ],
      #format : #lcov
    }
  }
}
