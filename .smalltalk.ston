SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {

      #baseline : 'FamixOpenApiGenerator',
      #directory : 'src',
      #ignoreImage : true,
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming
    }
  ],
  #testing : {
    #coverage : {
      #packages : [ 'FamixOpenApiGenerator.*' ],
      #format : #lcov
    }
  }
}
